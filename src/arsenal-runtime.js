import {ARSENAL_BY_ID,familyPower} from './arsenal.js';
const TAU=Math.PI*2;
const rt=(p,id)=>p._arsenalRt||(p._arsenalRt={}), key=>{};
const state=(p,id)=>p.arsenal?.[id]||{tier:0,branch:null,evo:0};
const lvl=(p,id)=>familyPower(state(p,id));
const ready=(p,id,dt,base)=>{const r=p._arsenalRt||(p._arsenalRt={});r[id]=(r[id]??Math.random()*base)-dt;if(r[id]<=0){r[id]=base;return true}return false};
const nearest=(enemies,x,y)=>{let b=null,d=Infinity;for(const e of enemies){if(e.dead)continue;const dd=(e.x-x)**2+(e.y-y)**2;if(dd<d){d=dd;b=e}}return b};
const strongest=enemies=>[...enemies].filter(e=>!e.dead).sort((a,b)=>(b.hp||0)-(a.hp||0))[0];
const shot=(shots,x,y,a,speed,damage,kind='arsenal',extra={})=>shots.push({x,y,vx:Math.cos(a)*speed,vy:Math.sin(a)*speed,r:extra.r||3,life:extra.life||1.5,damage,kind,pierce:extra.pierce||0,ricochet:extra.ricochet||0,target:extra.target,turn:extra.turn||0});
const area=(enemies,x,y,r,damage,hit)=>{const r2=r*r;for(const e of enemies)if(!e.dead&&(e.x-x)**2+(e.y-y)**2<r2)hit(e,damage)};
const fx=(p,type,data)=>{(p.arsenalFx||(p.arsenalFx=[])).push({type,life:data.life||.3,max:data.life||.3,...data})};
function profile(p,id){const s=state(p,id),f=ARSENAL_BY_ID[id],power=Math.max(1,familyPower(s)),branch=s.branch,e=s.evo||0;return{s,f,power,branch,e,mult:1+power*.13+(branch?e*.18:0)}}
export function tickArsenal(p,dt,c){const {enemies,shots,enemyShots,damageEnemy,elapsed}=c;const cfg=p.configurations||new Set();
 const has=id=>(state(p,id).tier||0)>0;
 // Kinetic families
 if(has('rail')&&ready(p,'rail',dt,Math.max(.38,1.15-lvl(p,'rail')*.08))){const q=profile(p,'rail'),e=nearest(enemies,p.x,p.y);if(e){const a=Math.atan2(e.y-p.y,e.x-p.x),n=q.branch==='b'?2+q.e:1;for(let i=0;i<n;i++)shot(shots,p.x,p.y,a+(i-(n-1)/2)*.07,900,18*q.mult*(q.branch==='a'?1.8:1),'rail',{pierce:Math.floor(q.power/2)+(cfg.has('gravity-spear')?2:0),r:q.branch==='a'?5:3});fx(p,'line',{x:p.x,y:p.y,a,len:220,life:.12})}}
 if(has('repeater')&&ready(p,'repeater',dt,Math.max(.07,.34-lvl(p,'repeater')*.022))){const q=profile(p,'repeater'),e=nearest(enemies,p.x,p.y);if(e){const a=Math.atan2(e.y-p.y,e.x-p.x),n=q.branch==='a'?2+Math.floor(q.e/2):1;for(let i=0;i<n;i++)shot(shots,p.x,p.y,a+(Math.random()-.5)*.06,700,7*q.mult*(q.branch==='b'?1.6:1),'repeater',{r:q.branch==='b'?5:2,ricochet:cfg.has('ballistic-cyclone')?1:0})}}
 if(has('scatter')&&ready(p,'scatter',dt,Math.max(.45,1.2-lvl(p,'scatter')*.07))){const q=profile(p,'scatter'),e=nearest(enemies,p.x,p.y);if(e){const a=Math.atan2(e.y-p.y,e.x-p.x),n=4+Math.floor(q.power)+(q.branch==='a'?q.e*3:0),spread=q.branch==='b'?.45:1.05;for(let i=0;i<n;i++)shot(shots,p.x,p.y,a+(i-(n-1)/2)*spread/Math.max(1,n-1),620,8*q.mult,'scatter',{pierce:q.branch==='b'?1+q.e:0})}}
 if(has('ricochet')&&ready(p,'ricochet',dt,Math.max(.55,1.5-lvl(p,'ricochet')*.08))){const q=profile(p,'ricochet'),e=nearest(enemies,p.x,p.y);if(e){const a=Math.atan2(e.y-p.y,e.x-p.x);shot(shots,p.x,p.y,a,580,14*q.mult,'ricochet',{ricochet:1+Math.floor(q.power/2)+(q.branch==='a'?q.e:0),r:q.branch==='b'?7:3})}}
 if(has('hunter')&&ready(p,'hunter',dt,Math.max(.4,1.35-lvl(p,'hunter')*.06))){const q=profile(p,'hunter'),target=q.branch==='b'?strongest(enemies):nearest(enemies,p.x,p.y);if(target){const n=1+Math.floor(q.power/2)+(q.branch==='a'?q.e*2:0);for(let i=0;i<n;i++)shot(shots,p.x,p.y,-Math.PI/2+(i-(n-1)/2)*.1,260,12*q.mult*(q.branch==='b'?1.7:1),'hunter',{target,turn:4.5,r:4,life:3})}}
 // Energy
 if(has('beam')&&ready(p,'beam',dt,Math.max(.28,.9-lvl(p,'beam')*.045))){const q=profile(p,'beam'),e=nearest(enemies,p.x,p.y);if(e){damageEnemy(e,15*q.mult*(q.branch==='a'?1.5:1));fx(p,'beam',{x:p.x,y:p.y,x2:e.x,y2:e.y,w:q.branch==='a'?8:2,life:.16});if(q.branch==='b')for(const e2 of enemies.filter(x=>x!==e).slice(0,q.e+1))damageEnemy(e2,8*q.mult)}}
 if(has('sweep')&&ready(p,'sweep',dt,Math.max(.8,2.4-lvl(p,'sweep')*.1))){const q=profile(p,'sweep'),r=160+q.power*18;area(enemies,p.x,p.y,r,14*q.mult,damageEnemy);fx(p,'ring',{x:p.x,y:p.y,r,life:.3});if(q.branch==='b')fx(p,'ring',{x:p.x,y:p.y,r:r*.72,life:.3})}
 if(has('prism')&&ready(p,'prism',dt,Math.max(.55,1.5-lvl(p,'prism')*.07))){const q=profile(p,'prism'),targets=[...enemies].filter(e=>!e.dead).sort((a,b)=>((a.x-p.x)**2+(a.y-p.y)**2)-((b.x-p.x)**2+(b.y-p.y)**2)).slice(0,2+Math.floor(q.power/2)+(q.branch==='a'?q.e*2:0));for(const e of targets){damageEnemy(e,9*q.mult);fx(p,'beam',{x:p.x,y:p.y,x2:e.x,y2:e.y,w:1,life:.12})}}
 if(has('nova')&&ready(p,'nova',dt,Math.max(.65,3-lvl(p,'nova')*.14))){const q=profile(p,'nova'),r=100+q.power*18+(q.branch==='a'?q.e*55:0);area(enemies,p.x,p.y,r,18*q.mult*(q.branch==='a'?1.4:1),damageEnemy);fx(p,'ring',{x:p.x,y:p.y,r,life:.35})}
 if(has('arc')&&ready(p,'arc',dt,Math.max(.45,1.7-lvl(p,'arc')*.075))){const q=profile(p,'arc'),targets=[...enemies].filter(e=>!e.dead).slice(0,2+Math.floor(q.power)+(q.branch==='a'?q.e*2:0));let x=p.x,y=p.y;for(const e of targets){damageEnemy(e,10*q.mult);fx(p,'beam',{x,y,x2:e.x,y2:e.y,w:2,life:.11});x=e.x;y=e.y}}
 // Melee + explosive
 if(has('orbit')){const q=profile(p,'orbit'),r=55+q.power*5,n=3+Math.floor(q.power)+(q.branch==='a'?q.e*3:0);for(const e of enemies)if(!e.dead){const d=Math.hypot(e.x-p.x,e.y-p.y);if(Math.abs(d-r)<12+(q.branch==='b'?12:0))damageEnemy(e,(7+q.power*1.5)*dt*5)}if(ready(p,'orbitFx',dt,.18))for(let i=0;i<n;i++){const a=elapsed*2.4+i*TAU/n;fx(p,'dot',{x:p.x+Math.cos(a)*r,y:p.y+Math.sin(a)*r,r:q.branch==='b'?8:4,life:.2})}}
 if(has('launchblade')&&ready(p,'launchblade',dt,Math.max(.65,1.8-lvl(p,'launchblade')*.08))){const q=profile(p,'launchblade'),e=nearest(enemies,p.x,p.y);if(e){const a=Math.atan2(e.y-p.y,e.x-p.x),n=q.power>5?2:1;for(let i=0;i<n;i++)shot(shots,p.x,p.y,a+(i?Math.PI*.08:0),500,16*q.mult,'blade',{pierce:2+(q.branch==='b'?4:0),r:6,life:2})}}
 if(has('slash')&&ready(p,'slash',dt,Math.max(.45,1.4-lvl(p,'slash')*.07))){const q=profile(p,'slash'),e=nearest(enemies,p.x,p.y);if(e){const a=Math.atan2(e.y-p.y,e.x-p.x),range=125+q.power*15;for(const t of enemies)if(!t.dead){const da=Math.atan2(t.y-p.y,t.x-p.x),d=Math.hypot(t.x-p.x,t.y-p.y);if(d<range&&Math.abs(Math.atan2(Math.sin(da-a),Math.cos(da-a)))<(q.branch==='a'?1.0:.65))damageEnemy(t,20*q.mult)}fx(p,'arc',{x:p.x,y:p.y,a,r:range,life:.18})}}
 if(has('drill')&&ready(p,'drill',dt,Math.max(.5,1.5-lvl(p,'drill')*.07))){const q=profile(p,'drill'),e=q.branch==='a'?strongest(enemies):nearest(enemies,p.x,p.y);if(e){damageEnemy(e,24*q.mult*(q.branch==='a'?1.7:1));fx(p,'beam',{x:p.x,y:p.y,x2:e.x,y2:e.y,w:5,life:.2})}}
 if(has('ram')&&p.dashTime>0){const q=profile(p,'ram');for(const e of enemies)if(!e.dead&&!e._arsenalRam&&Math.hypot(e.x-p.x,e.y-p.y)<e.r+p.r+18){e._arsenalRam=true;damageEnemy(e,24*q.mult)}if(q.branch==='b'&&ready(p,'phantom',dt,.12))fx(p,'dot',{x:p.x-p.dashDir.x*34,y:p.y-p.dashDir.y*34,r:10,life:.35})}else for(const e of enemies)e._arsenalRam=false;
 if(has('missile')&&ready(p,'missile2',dt,Math.max(.45,1.8-lvl(p,'missile')*.08))){const q=profile(p,'missile'),target=nearest(enemies,p.x,p.y);if(target){const n=2+Math.floor(q.power/2)+(q.branch==='a'?q.e*3:0);for(let i=0;i<n;i++)shot(shots,p.x,p.y,-Math.PI/2+(i-(n-1)/2)*.08,220,15*q.mult*(q.branch==='b'?1.6:1),'missile',{target,turn:4,r:q.branch==='b'?7:4,life:3})}}
 if(has('mortar')&&ready(p,'mortar',dt,Math.max(.7,2.7-lvl(p,'mortar')*.11))){const q=profile(p,'mortar'),e=nearest(enemies,p.x,p.y);if(e){const r=55+q.power*5+(q.branch==='b'?q.e*28:0);area(enemies,e.x,e.y,r,22*q.mult,damageEnemy);fx(p,'ring',{x:e.x,y:e.y,r,life:.35})}}
 if(has('cluster')&&ready(p,'cluster',dt,Math.max(.65,2.1-lvl(p,'cluster')*.09))){const q=profile(p,'cluster'),e=nearest(enemies,p.x,p.y);if(e){const n=5+Math.floor(q.power)+(q.branch==='a'?q.e*4:0),a0=Math.atan2(e.y-p.y,e.x-p.x);for(let i=0;i<n;i++)shot(shots,p.x,p.y,a0+(i-(n-1)/2)*.18,420,8*q.mult,'cluster',{pierce:state(p,'cluster').tier>=4?1:0})}}
 if(has('mine')&&ready(p,'mine2',dt,Math.max(.75,2.6-lvl(p,'mine')*.11))){const q=profile(p,'mine'),n=1+Math.floor(q.power/3)+(q.branch==='a'?q.e:0);for(let i=0;i<n;i++)shots.push({x:p.x+(Math.random()-.5)*28,y:p.y+(Math.random()-.5)*28,vx:0,vy:0,r:q.branch==='b'?12:8,life:7,damage:20*q.mult,kind:'mine',pierce:0})}
 if(has('plasma')&&ready(p,'plasma',dt,Math.max(.65,2.2-lvl(p,'plasma')*.1))){const q=profile(p,'plasma'),e=nearest(enemies,p.x,p.y);if(e){const r=50+q.power*6+(q.branch==='a'?q.e*25:0);area(enemies,e.x,e.y,r,8*q.mult,damageEnemy);fx(p,'zone',{x:e.x,y:e.y,r,life:q.branch==='a'?1.3:.6})}}
 // Autonomous, defense, control
 if(has('drone')&&ready(p,'drone2',dt,Math.max(.22,.75-lvl(p,'drone')*.03))){const q=profile(p,'drone'),e=nearest(enemies,p.x,p.y);if(e){const n=1+Math.floor(q.power/2)+(q.branch==='a'?q.e*2:0);for(let i=0;i<n;i++){const a=i*TAU/n+elapsed;shot(shots,p.x+Math.cos(a)*60,p.y+Math.sin(a)*60,Math.atan2(e.y-p.y,e.x-p.x),560,7*q.mult,'drone')}}}
 if(has('funnels')&&ready(p,'funnels',dt,Math.max(.25,.85-lvl(p,'funnels')*.03))){const q=profile(p,'funnels'),n=2+Math.floor(q.power/2)+(q.branch==='a'?q.e*2:0);for(let i=0;i<n;i++){const e=enemies[(i*3)%Math.max(1,enemies.length)];if(e&&!e.dead){const a=i*TAU/n+elapsed*.8,x=p.x+Math.cos(a)*95,y=p.y+Math.sin(a)*95;shot(shots,x,y,Math.atan2(e.y-y,e.x-x),600,6*q.mult,'funnel')}}}
 if(has('sentry')&&ready(p,'sentry',dt,Math.max(.3,.95-lvl(p,'sentry')*.035))){const q=profile(p,'sentry'),e=nearest(enemies,p.x,p.y);if(e){const n=q.branch==='b'?1:1+Math.floor(q.power/3);for(let i=0;i<n;i++)shot(shots,p.x+(i-(n-1)/2)*24,p.y+42,Math.atan2(e.y-p.y-42,e.x-p.x),520,8*q.mult*(q.branch==='b'?1.8:1),'sentry')}}
 if(has('interceptor')){const q=profile(p,'interceptor'),r=90+q.power*8;for(const s of enemyShots)if(!s.dead&&Math.hypot(s.x-p.x,s.y-p.y)<r){s.dead=true;if(q.branch==='b'){const e=nearest(enemies,s.x,s.y);if(e)shot(shots,s.x,s.y,Math.atan2(e.y-s.y,e.x-s.x),520,9*q.mult,'counter')}break}}
 if(has('barrier')){const q=profile(p,'barrier'),r=60+q.power*6;for(const e of enemies)if(!e.dead&&Math.hypot(e.x-p.x,e.y-p.y)<r)damageEnemy(e,5*q.mult*dt*5);if(ready(p,'barrierFx',dt,.2))fx(p,'ring',{x:p.x,y:p.y,r,life:.22})}
 if(has('graviton')&&ready(p,'graviton',dt,.06)){const q=profile(p,'graviton'),r=130+q.power*12;for(const e of enemies)if(!e.dead){const dx=p.x-e.x,dy=p.y-e.y,d=Math.hypot(dx,dy)||1;if(d<r){const force=(q.branch==='a'?120:55)+q.power*8;e.x+=dx/d*force*dt;e.y+=dy/d*force*dt;if(d<35)damageEnemy(e,5*q.mult*dt*4)}}}
 if(has('repulsor')&&ready(p,'repulsor',dt,Math.max(.7,2.2-lvl(p,'repulsor')*.08))){const q=profile(p,'repulsor'),r=125+q.power*10;for(const e of enemies)if(!e.dead){const dx=e.x-p.x,dy=e.y-p.y,d=Math.hypot(dx,dy)||1;if(d<r){const f=(q.branch==='b'?55:90)+q.power*5;e.x+=dx/d*f;e.y+=dy/d*f;damageEnemy(e,7*q.mult)}}fx(p,'ring',{x:p.x,y:p.y,r,life:.3})}
 if(has('mark')&&ready(p,'mark',dt,Math.max(1.5,5-lvl(p,'mark')*.18))){for(const e of enemies)e._execMark=false;const q=profile(p,'mark'),targets=[...enemies].filter(e=>!e.dead).sort((a,b)=>(b.hp||0)-(a.hp||0)).slice(0,q.branch==='b'?1:1+Math.floor(q.power/4));for(const e of targets){e._execMark=true;e._execMarkMult=1.18+q.power*.04+(q.branch==='a'?.35:0);fx(p,'ring',{x:e.x,y:e.y,r:e.r+10,life:.45})}}
 // Exotic: infer kills since previous tick.
 const prev=p._arsenalKills??p.kills,delta=Math.max(0,p.kills-prev);p._arsenalKills=p.kills;if(delta>0&&has('death')){const q=profile(p,'death'),n=Math.min(6,delta+(q.branch? q.e:0));for(let i=0;i<n;i++){const e=enemies[i%Math.max(1,enemies.length)];if(e&&!e.dead){if(q.branch==='b')shot(shots,p.x,p.y,Math.atan2(e.y-p.y,e.x-p.x),520,10*q.mult,'soul',{target:e,turn:3.5});else{area(enemies,e.x,e.y,55+q.power*3,10*q.mult,damageEnemy);fx(p,'ring',{x:e.x,y:e.y,r:55+q.power*3,life:.25})}}}}
 if(has('temporal')&&ready(p,'temporal',dt,Math.max(.7,2.5-lvl(p,'temporal')*.08))){const q=profile(p,'temporal'),targets=[...enemies].filter(e=>!e.dead).slice(0,1+q.e);for(const e of targets){damageEnemy(e,9*q.mult);fx(p,'beam',{x:p.x,y:p.y,x2:e.x,y2:e.y,w:2,life:.4})}if(cfg.has('aftershock')&&has('nova')){const r=90+lvl(p,'nova')*10;area(enemies,p.x,p.y,r,10*q.mult,damageEnemy);fx(p,'ring',{x:p.x,y:p.y,r,life:.4})}}
 // Selected cross-system hooks.
 if(cfg.has('seraph-wing')&&ready(p,'cfg-seraph',dt,1.1)){const e=nearest(enemies,p.x,p.y);if(e)for(let i=0;i<2;i++)shot(shots,p.x,p.y,-Math.PI/2+i*.12,240,10,'missile',{target:e,turn:4,life:3})}
 if(cfg.has('mine-drive')&&p.dashTime>0&&ready(p,'cfg-mine',dt,.12))shots.push({x:p.x,y:p.y,vx:0,vy:0,r:8,life:5,damage:20,kind:'mine',pierce:0});
 if(cfg.has('extinction-chain')&&delta>0&&enemies[0])for(const e of enemies.slice(0,Math.min(4,delta+2)))if(!e.dead)damageEnemy(e,7+delta*2);
}
export function applyMarkedDamage(e,d){return d*(e?._execMarkMult||1)}
export function updateArsenalFx(p,dt){for(const f of p.arsenalFx||[])f.life-=dt;p.arsenalFx=(p.arsenalFx||[]).filter(f=>f.life>0)}
export function drawArsenalFx(ctx,p,palette){for(const f of p.arsenalFx||[]){ctx.save();ctx.globalAlpha=Math.max(0,f.life/f.max);ctx.strokeStyle=palette.cyan;ctx.fillStyle='rgba(22,143,255,.12)';ctx.shadowColor=palette.cyan;ctx.shadowBlur=10;if(f.type==='beam'||f.type==='line'){ctx.lineWidth=f.w||2;ctx.beginPath();ctx.moveTo(f.x,f.y);if(f.x2!=null)ctx.lineTo(f.x2,f.y2);else ctx.lineTo(f.x+Math.cos(f.a||0)*(f.len||140),f.y+Math.sin(f.a||0)*(f.len||140));ctx.stroke()}else if(f.type==='ring'||f.type==='zone'){ctx.beginPath();ctx.arc(f.x,f.y,f.r||30,0,TAU);f.type==='zone'?ctx.fill():ctx.stroke()}else if(f.type==='arc'){ctx.lineWidth=3;ctx.beginPath();ctx.arc(f.x,f.y,f.r||80,(f.a||0)-.65,(f.a||0)+.65);ctx.stroke()}else{ctx.beginPath();ctx.arc(f.x,f.y,f.r||4,0,TAU);ctx.fill()}ctx.restore()}}
